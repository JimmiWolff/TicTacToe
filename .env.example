# Auth0 Configuration
AUTH0_DOMAIN=your-auth0-domain.auth0.com
AUTH0_CLIENT_ID=your-client-id
AUTH0_CLIENT_SECRET=your-client-secret
AUTH0_AUDIENCE=your-api-identifier

# Application Configuration
NODE_ENV=development
PORT=3000

# Database (for future use if needed)
# DATABASE_URL=your-database-url

# Session Secret
SESSION_SECRET=your-super-secret-key-change-this-in-production

# Sentry Configuration
# Get your DSN from https://sentry.io/settings/projects/your-project/keys/
SENTRY_DSN=https://your-dsn@sentry.io/your-project-id
# Optional: Use separate DSN for public client-side (with only browser scope)
# SENTRY_DSN_PUBLIC=https://your-public-dsn@sentry.io/your-project-id
# Optional: Enable Sentry in development (default: only enabled in production)
# SENTRY_ENABLED=true

# Apple Sign In Configuration
APPLE_CLIENT_ID=com.yourcompany.tictactoe
APPLE_TEAM_ID=XXXXXXXXXX
APPLE_KEY_ID=XXXXXXXXXX

# Option 1: Store private key content directly (RECOMMENDED for Railway/cloud deployments)
# Copy the entire content of your AuthKey_XXXXXXXXXX.p8 file including the BEGIN/END lines
APPLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour key content here\n-----END PRIVATE KEY-----"

# Option 2: Or use a file path (for local development)
# APPLE_PRIVATE_KEY_PATH=/path/to/AuthKey_KEYID.p8